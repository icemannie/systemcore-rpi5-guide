"""SystemCore Pi 5B image patcher.

Apply Pi 5B-compatible modifications to an existing upstream SystemCore image.
Both `build-image.sh` (downloads + patches from scratch) and `patch-image.py`
(patches an existing image) use this package.
"""

__version__ = "1.0.0"
